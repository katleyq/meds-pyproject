# pyproject.toml
[project]
name = "ocean-temp-analysis"
version = "0.1.0"
description = "Analyze ocean temperature trends"
dependencies = [
    "pandas>=2.0",
    "matplotlib>=3.7",
    "numpy>=1.24"
]

[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"